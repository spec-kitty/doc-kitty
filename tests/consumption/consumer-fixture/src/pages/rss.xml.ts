import { rssRoute } from '@commondocs-kitty/toolkit/routes';

export const GET = rssRoute({
  title: 'Consumer Fixture',
  description: 'Documentation updates from the clean-room consumer fixture.',
});
