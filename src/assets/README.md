# Doc Kitty toolkit assets

Ships shared static assets consumed by the chrome carriers and layouts (exported
via `@commondocs-kitty/toolkit/assets/*`). WP03 adds the site-default social
image here (the last fallback for `og:image` / `twitter:image`). Kept as a
tracked directory so the `./assets/*` package export always resolves.
