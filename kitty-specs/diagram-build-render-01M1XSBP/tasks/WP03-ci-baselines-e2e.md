---
work_package_id: WP03
title: CI/deploy infra + baselines + a11y + deck
dependencies:
- WP01
- WP02
requirement_refs:
- FR-009
- FR-010
- FR-011
- NFR-003
- NFR-004
- NFR-005
planning_base_branch: feat/diagram-build-render
merge_target_branch: feat/diagram-build-render
branch_strategy: Planning artifacts for this mission were generated on feat/diagram-build-render. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/diagram-build-render unless the human explicitly redirects the landing branch.
subtasks:
- T201
- T202
- T203
history: []
agent_profile: implementer-ivan
authoritative_surface: tests/a11y/diagram.spec.ts
create_intent: []
execution_mode: code_change
owned_files:
- .github/workflows/ci.yml
- .github/workflows/deploy.yml
- .github/actions/build-example/action.yml
- tests/a11y/diagram.spec.ts
- src/layouts/DeckLayout.astro
role: implementer
tags: []
tracker_refs: []
---

## Objective
Issue #13, WP03: make CI/deploy BUILD-RENDER both engines, regenerate/verify the
pinned baselines under the build engines, make the diagram e2e (`diagram.spec.ts`)
MODE-AWARE, and give the out-of-frame DECK a build-mode static-SVG path (client
render preserved as the fallback). WP01 (Mermaid, b84067a) + WP02 (PlantUML,
2be2ef1) are committed. Read kitty-specs/diagram-build-render-01M1XSBP/
{spec,plan,research}.md, contracts/build-render.md. Issue: `gh issue view 13`.

## Environment (local verification IS possible)
- Chromium at `~/.cache/ms-playwright` (set `PLAYWRIGHT_BROWSERS_PATH`). A
  self-hosted PlantUML server runs at http://localhost:8091 (`docker ps` →
  dk-plantuml-spike; restart: `docker run -d --name dk-plantuml -p 8091:8080
  plantuml/plantuml-server:jetty`). `DEFAULT_PLANTUML_SERVER_URL` in config.ts is
  http://localhost:8091/svg/ — CI overrides it via env to the CI service.
- **Mode-switch cache-trap** (known hazard): `pnpm clean` between a build-mode and
  a client-mode build, else a stale `example/.astro` fakes results.
- Unit: `cd src && ./node_modules/.bin/vitest run --project fast`. e2e:
  `pnpm exec playwright test --output <scratch>` (test-results/ is root-owned).
  Do NOT run `astro check`. Do NOT commit — the orchestrator commits.

## Subtasks
### T201 — CI + deploy build-render infra + @beoe cache
- The build-example composite (`.github/actions/build-example/action.yml`) and the
  jobs that use it (`ci.yml` build-example job, `deploy.yml`) must build-render:
  - Chromium: add `npx playwright install --with-deps chromium` before the build
    (the composite runs on bare ubuntu; the runner has sudo). Pin consistent with
    `@playwright/test@1.62.1`.
  - PlantUML: add a job-level `services: plantuml: image: plantuml/plantuml-server:jetty`
    (services are job-level, NOT in a composite) to the build-example job AND the
    deploy job; set env `DK_DIAGRAM_BUILD_RENDER=on` +
    `DK_PLANTUML_SERVER_URL=http://localhost:<mapped>/svg/` (or the service host).
  - @beoe disk cache: configure `@beoe/rehype-mermaid`'s cache dir (config.ts
    `BUILD_MERMAID_OPTS`) to a stable path and add an `actions/cache` step keyed on
    the diagram sources so unchanged diagrams skip Chromium (FR-009). Verify a
    warm-cache rebuild does not relaunch Chromium.
  - The a11y lane already runs in the pinned Playwright container and consumes the
    build-example dist — so after this, a11y sees BUILD-mode diagrams.
### T202 — baselines under the build engines
- Assess the pinned visual baselines (`tests/a11y/__screenshots__/`, `visual.spec.ts`):
  regenerate ONLY those that actually change under build-render (the brand-home
  baseline has no diagram → unchanged; a diagram/deck screenshot would change).
  Pin the regenerated bytes. The render-time engine versions are exact-pinned, so
  bytes are stable; note any nondeterminism and pin it.
### T203 — mode-aware diagram.spec.ts + build-mode deck
- Rewrite `tests/a11y/diagram.spec.ts` to be MODE-AWARE (it runs against whatever
  mode the consumed dist was built in — CI build-example is now build mode):
  - FP-1/NFR-006: build mode asserts NO `/mermaid/i` chunk/request on a diagram
    page (invert the client "chunk IS fetched"); T022 (diagram-free deck zero
    mermaid) holds in both.
  - DX-2/DX-5: the build `<svg>` is named via `<title>`/`<desc>` + figure
    aria-labelledby; caption present. Covers Mermaid AND the new PlantUML page.
  - T021/DX-4 theme: build mode is PURE CSS — assert the computed node fill EQUALS
    the other mode's `--dk-diagram-node-fill` after a `[data-theme]` toggle with NO
    re-render and still exactly one `<svg>` per figure (no client re-render path).
  - Deck (T019/T020/T021): in build mode the deck slides carry static SVGs; assert
    them present/named without the client slide-by-slide render machinery.
- **Deck build-mode path** (`src/layouts/DeckLayout.astro` + as needed the render
  owner): in build mode the deck's ```mermaid becomes a static `<svg>` like docs,
  so the client `initDiagrams` must be a NO-OP / skipped for build-rendered slides
  (a static SVG needs no settle); the reveal INV-SETTLE/SCOPE machinery stays ONLY
  for the client fallback. Do not break the client-fallback deck.
### T204 — deck + multi-diagram gate hardening
- Extend `assert-build-artifacts.mjs` so the DECK build figure is asserted like the
  docs build figure (inline svg + name + var() + no chromatic hex) in build mode,
  client shape in client mode.
- **renata WP02 INFO (fold here, in-mission):** a failed astro-plantuml render
  emits a `<div class="plantuml-error">` (not a figure), and for a page with
  MULTIPLE plantuml diagrams the figure-builder's document-order index can
  misalign against the `file.data.dkPlantuml` stash when an error node is skipped.
  Add a guard/test: the figure index must key off the actual rendered figures, and
  a render error must fail the build loudly (the gate already fails on a missing
  figure — make the index alignment robust for the multi-diagram case).

## Verify & report
- Local: build-mode build (server+Chromium) → run the mode-aware `diagram.spec.ts`
  (both colour modes) green; deck slides show static named themed SVGs; a
  warm-cache rebuild skips Chromium. Client-mode build (after `pnpm clean`) → the
  e2e client branch green, deck client-renders, Mermaid byte-identical.
- CI YAML changes can only fully verify on the PR run — report what you changed and
  why. Report per-file changes, the CI infra diff, baseline decisions, the deck
  build-mode mechanism, e2e mode-branch summary, and the multi-diagram fix. Do NOT commit.

## Definition of Done
- CI+deploy build-render both engines with a warm cache; baselines pinned; the
  diagram e2e is mode-aware and green in build mode (the CI mode) AND client mode;
  the deck has a build-mode static-SVG path with the client fallback intact; deck +
  multi-diagram gates green. Docs/ADR are WP04.
