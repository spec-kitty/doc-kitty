---
title: Metadata model + chrome
description: "The frontmatter contract every page carries, plus the metadata-driven page chrome the toolkit renders from it."
status: draft
updated: 2026-08-22
type: Feature
moscow:
  level: Must
  rationale: The contract every other feature reads and renders from.
tags: [metadata, chrome]
related:
  - architecture/metadata-model
  - adr/0009-finalize-metadata-contract
---

# Metadata model + chrome

Mission M1. The frontmatter contract each page carries and the page chrome driven
by it: navigation, related blocks, share metadata, and validation.

Scope: MVP.

Design: [metadata model](../../architecture/metadata-model.md) and
[ADR-0009](../../adr/0009-finalize-metadata-contract.md).
